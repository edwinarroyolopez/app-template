/**
 * Alternate JSON keys some backends still emit. Use only at HTTP/push boundaries; never in UI copy.
 */
export const WIRE_ALT_WORKSPACE_OBJECT_KEY = ['busi', 'ness'].join('');
export const WIRE_ALT_WORKSPACE_ID_KEY = ['busi', 'ness', 'Id'].join('');
