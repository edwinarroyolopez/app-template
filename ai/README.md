# AI governance (deprecated path)

Canonical agent workflow, instructions, agents, and skills live in the **repository root**:

`../../ai/`

Do not maintain a second copy under `template-app/ai/`. Open the root `ai/` tree from your editor or symlink it locally if you prefer a shorter path.
